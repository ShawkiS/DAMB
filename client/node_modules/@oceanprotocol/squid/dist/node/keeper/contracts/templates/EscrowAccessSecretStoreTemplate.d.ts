import { AgreementTemplate } from "./AgreementTemplate.abstract";
import { DDO } from "../../../ddo/DDO";
import { InstantiableConfig } from "../../../Instantiable.abstract";
export declare class EscrowAccessSecretStoreTemplate extends AgreementTemplate {
    static getInstance(config: InstantiableConfig): Promise<EscrowAccessSecretStoreTemplate>;
    getServiceAgreementTemplate(): Promise<import("../../../ddo/ServiceAgreementTemplate").ServiceAgreementTemplate>;
    createAgreement(agreementId: string, did: string, conditionIds: string[], timeLocks: number[], timeOuts: number[], accessConsumer: string, from?: string): any;
    createAgreementFromDDO(agreementId: string, ddo: DDO, consumer: string, from?: string): Promise<boolean>;
    getAgreementIdsFromDDO(agreementId: string, ddo: DDO, consumer: string, from?: string): Promise<string[]>;
    createFullAgreement(did: string, amount: number, consumer: string, from?: string, agreementId?: string): Promise<string>;
    private createFullAgreementData;
}
