import ContractBase from "./ContractBase";
import { InstantiableConfig } from "../../Instantiable.abstract";
export default class OceanToken extends ContractBase {
    static getInstance(config: InstantiableConfig): Promise<OceanToken>;
    approve(to: string, price: number, from?: string): Promise<any>;
    decimals(): Promise<number>;
    balanceOf(address: string): Promise<number>;
    transfer(to: string, amount: number, from: string): Promise<any>;
}
