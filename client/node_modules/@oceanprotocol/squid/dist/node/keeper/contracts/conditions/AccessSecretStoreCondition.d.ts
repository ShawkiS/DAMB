import { Condition } from "./Condition.abstract";
import { InstantiableConfig } from "../../../Instantiable.abstract";
export declare class AccessSecretStoreCondition extends Condition {
    static getInstance(config: InstantiableConfig): Promise<AccessSecretStoreCondition>;
    hashValues(did: string, grantee: string): Promise<string>;
    fulfill(agreementId: string, did: string, grantee: string, from?: string): any;
    checkPermissions(grantee: string, did: string, from?: string): Promise<boolean>;
}
