import ContractBase from "../ContractBase";
import { InstantiableConfig } from "../../../Instantiable.abstract";
export interface AgreementData {
    did: string;
    didOwner: string;
    templateId: string;
    conditionIds: string[];
    lastUpdatedBy: string;
    blockNumberUpdated: number;
}
export declare class AgreementStoreManager extends ContractBase {
    static getInstance(config: InstantiableConfig): Promise<AgreementStoreManager>;
    getOwner(): Promise<string>;
    getAgreement(agreementId: string): Promise<AgreementData>;
}
