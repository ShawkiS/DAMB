import ContractBase from "../ContractBase";
import { ConditionState } from "../conditions/Condition.abstract";
import { InstantiableConfig } from "../../../Instantiable.abstract";
export interface ConditionData {
    typeRef: string;
    state: ConditionState;
    timeLock: number;
    timeOut: number;
    blockNumber: number;
    lastUpdatedBy: string;
    blockNumberUpdated: number;
}
export declare class ConditionStoreManager extends ContractBase {
    static getInstance(config: InstantiableConfig): Promise<ConditionStoreManager>;
    getOwner(): Promise<string>;
    getCondition(conditionId: string): Promise<ConditionData>;
}
