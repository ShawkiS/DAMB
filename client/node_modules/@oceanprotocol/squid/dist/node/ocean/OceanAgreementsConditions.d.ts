import Account from "./Account";
import { Instantiable, InstantiableConfig } from "../Instantiable.abstract";
export declare class OceanAgreementsConditions extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<OceanAgreementsConditions>;
    lockReward(agreementId: string, amount: number, from?: Account): Promise<boolean>;
    grantAccess(agreementId: string, did: string, grantee: string, from?: Account): Promise<boolean>;
    releaseReward(agreementId: string, amount: number, did: string, consumer: string, publisher: string, from?: Account): Promise<boolean>;
}
