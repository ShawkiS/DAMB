import Account from "./Account";
import { Instantiable, InstantiableConfig } from "../Instantiable.abstract";
import { OceanAgreementsConditions } from "./OceanAgreementsConditions";
export interface AgreementPrepareResult {
    agreementId: string;
    signature: string;
}
export declare class OceanAgreements extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<OceanAgreements>;
    conditions: OceanAgreementsConditions;
    prepare(did: string, serviceDefinitionId: string, consumer: Account): Promise<AgreementPrepareResult>;
    send(did: string, agreementId: string, serviceDefinitionId: string, signature: string, consumer: Account): Promise<void>;
    create(did: string, agreementId: string, serviceDefinitionId: string, signature: string, consumer: Account, publisher: Account): Promise<boolean>;
}
