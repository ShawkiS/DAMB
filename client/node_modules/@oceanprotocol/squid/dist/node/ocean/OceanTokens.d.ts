import Account from "./Account";
import { Instantiable, InstantiableConfig } from "../Instantiable.abstract";
export declare class OceanTokens extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<OceanTokens>;
    transfer(to: string, amount: number, from: Account): Promise<boolean>;
    request(account: Account, amount: number): Promise<boolean>;
}
