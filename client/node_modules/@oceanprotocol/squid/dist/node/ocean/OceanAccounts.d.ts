import Balance from "../models/Balance";
import Account from "./Account";
import { Instantiable, InstantiableConfig } from "../Instantiable.abstract";
export declare class OceanAccounts extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<OceanAccounts>;
    list(): Promise<Account[]>;
    balance(account: Account): Promise<Balance>;
    requestTokens(account: Account, amount: number): Promise<boolean>;
}
